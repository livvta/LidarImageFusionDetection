# 1 绪论
## 1.1 研究背景与意义
## 1.2 国内外研究现状
### 1.2.1 图像目标检测技术的研究现状
### 1.2.2 点云目标检测技术的研究现状
### 1.2.3 传感器融合的目标检测技术研究现状
### 1.2.4 无人控制平台研究现状
## 1.3 本文的组织架构


# 2 多传感器的时空同步
## 2.1 传感器基础理论 （可不写）
### 2.1.1 激光雷达检测原理 （可不写）
### 2.1.2 相机成像原理 （可不写）

## 2.2 多传感器空间同步
### 2.2.1 坐标系及边界框概述
（1）激光雷达坐标系
激光雷达坐标系是适用于室外场景点云检测普遍采用的激光雷达设备的坐标系。在该坐标系中 z 轴负方向指向地面，x 轴正方向指向前方，y 轴正方向指向左侧。

在本项目中，所有坐标系都是右手坐标系，这意味着如果从重力轴的负方向（轴的正方向指向人眼）看，转向角 (yaw) 沿着逆时针方向增加。

（2）相机坐标系
相机坐标系代表彩色相机常用的坐标系。在该坐标系中 y 轴正方向指向地面，x 轴正方向指向右侧，z 轴正方向指向前方。在本项目中，相机坐标系是三维点云与二维图像之间的桥梁。
### 2.2.2 激光雷达与相机的坐标系转换
### 2.2.3 相机的内参标定
### 2.2.4 激光雷达和相机联合标定
### 2.2.5 结果 3d22d &amp; 点云投影图
讲解 3D检测框坐标系变换，投影。得到8个角点。

## 2.3 多传感器时间同步
### 2.3.1 采用的方法
### 2.3.2 ROS自带的时间同步


# 3 基于激光雷达点云的目标检测算法
## 3.1 常见的点云检测算法 （略写）

## 3.2 PointPliiars目标检测算法
### 3.2.x PointPliiars的各个模块介绍

## 3.3 数据集选择与预处理

本实验选择KITTI点云数据集进行PointPillars模型训练。KITTI数据集由德国卡尔斯鲁厄理工学院（KIT）与丰田技术研究所（TTIC）于2012年联合发布，旨在推动自动驾驶与计算机视觉领域的研究。该数据集包含真实世界中的图像和激光雷达数据，涵盖城市、乡村和高速公路等多种复杂场景。KITTI数据集广泛应用于道路目标检测、自动驾驶定位与导航等任务，成为计算机视觉领域的重要基准数据集之一。

### 3.3.1 有效区域划分
激光雷达发射红外光束，接收器接收到周围物体反射回来的激光，对周围物体进行全面的收集，同时会反射回来大量的点云数据，其中包含了大量的地面、建筑物等干扰点云，对于远处的物体扫描得到的点云数量比较稀疏，如果使用采集的原始点云数据直接进行计算会造成算法的实时性降低，还会消耗许多不必要的计算。本文通过对激光雷达点云数据划分感兴趣区域、点云滤波和对地面点云进行滤除等方法来减少点云的数量。

本实验划分感兴趣区域的具体范围如表 所示。

### 3.3.2 点云按线序下采样
 
 参考 情况说明.doc

## 3.4 算法实验验证及分析
### 3.4.1 评价指标
### 3.4.2 实验环境设置
### 3.4.3 算法性能分析 (64, 16 比较)
### 3.4.4 检测结果可视化分析


# 4 基于图像的目标检测算法
## 4.1 常见的图像检测算法 （略写）
### 4.1.1 传统的图像检测算法
### 4.1.2 基于深度学习的图像检测算法

## 4.2 YOLOV11/5目标检测算法
### 4.2.x YOLO的各个模块

## 4.3 算法实验验证及分析
### 4.3.1 评价指标 （略讲三分类方法）
### 4.3.2 实验环境设置
### 4.3.3 算法性能分析
### 4.3.4 检测结果可视化分析
介绍结果的BoundBoxArray2D格式


# 5 基于激光雷达与视觉融合的目标检测（题目待定）
在第3、4章中，研究了点云的目标检测以及图像的目标检测。从实验结果可以看出，基于视觉的目标检测算法虽然能提供丰富的视觉信息，但容易受到环境因素（如遮挡、光照等）的干扰。而激光雷达虽然可以提供高精度的3D位置信息，但无法提供颜色和纹理等视觉信息，因此其在目标识别和分类方面存在局限性。
针对这一挑战，本章提出了一种低复杂度的决策级融合算法，旨在解决在轻量化平台上由于计算资源受限导致的高复杂度算法无法应用的问题。通过将激光雷达的点云数据与相机的图像信息进行有效融合，该算法能够弥补各自单独使用时的不足，实现信息互补，从而显著提高目标检测的精度。

## 5.1 多传感器融合方法
### 5.1.1 数据级融合
数据级融合是在传感器原始数据层面进行融合，通过将来自不同传感器的数据（如激光雷达的点云数据和摄像头的图像数据）直接对齐，生成统一的数据表示。该方法的优点在于充分利用了传感器提供的原始信息，能够最大程度地保留各传感器的数据特性和细节。
然而，数据级融合面临着多个挑战，包括传感器间数据格式的差异、数据同步的复杂性以及对数据对齐精度的严格要求。尽管这种方法能够提供精确的环境感知，但其计算量大，对实时系统的硬件性能和算法效率提出了较高的要求，在实际的道路目标检测并不常见。

图5.1 数据级融合流程图

### 5.1.2 特征级融合
特征级融合是在从不同传感器获取数据之后，先提取各自的特征信息，再进行融合。与数据级融合相比，特征级融合将重点放在提取有意义的特征上，避免了直接操作原始数据的复杂性。通过提取图像和点云数据的特征（如边缘、纹理、形状等），可以有效地将两种模态的信息结合起来，用于进一步的目标检测与分类。 
这种方法在保留关键信息的同时，减少了原始数据的冗余，平衡了精度和效率。特征级融合计算量适中，实时性相对较好，适合目标检测和定位任务。然而，效果依赖于特征提取的质量，如果特征提取不准确，可能导致信息丢失或融合效果下降。 

图5.2 特征级融合流程图

### 5.1.3 决策级融合
决策级融合是指在各个传感器完成目标检测或分类任务之后，结合不同传感器的检测结果进行决策。这一方法通常将每个传感器的独立检测结果视为一个单独的“决策”进行处理，最后通过加权投票、置信度加权等方式来决定最终的检测结果。决策级融合的优点是处理的数据量远小于数据层或特征层融合，且可以在保持传感器独立性的同时，充分发挥不同传感器的优势。
此方法计算量最低，鲁棒性强，适应性和灵活性较高，能够容忍部分传感器失效。决策级融合适用于实时性要求高的场景，但因仅利用最终决策结果，信息利用率较低。

图5.3 决策级融合流程图

### 5.1.4 总结
方法总结如下表：

| **融合方法**      | **数据级融合**   | **特征级融合** | **决策级融合**   |
|--------------------|----------------|----------------|----------------|
| **融合层次**      | 低层次           | 中等层次       | 高层次             |
| **输入信息**      | 原始传感器信息    | 特征向量       | 各传感器的检测结果  |
| **计算量**        | 大               | 中            | 小                 |
| **抗干扰能力**    | 弱               | 中等           | 强                |

本文最终采用决策层融合方法，将图像和点云的检测结果进行融合，从而来满足车辆在道路上对目标识别的实时性和检测的精准性。

## 5.2 多传感器检测结果关联
在多传感器融合的目标检测中，关联来自不同传感器的检测结果是一个至关重要的步骤。为了有效地将激光雷达和视觉传感器的检测结果结合，通常需要使用一些评估指标和后处理方法来提高检测结果的准确性和一致性。常用的方法包括交并比（IoU）和非极大值抑制（NMS）等。以下分别介绍这些方法及其在多传感器检测结果关联中的应用。
### 5.2.1 交并比算法
交并比（Intersection over Union，IoU）是目标检测中常用的一种评估指标，表示两个边界框的交集与并集的比值。IoU通常用于衡量预测框和真实框之间的重叠程度，用于确定检测结果是否为有效的检测。
对于两个边界框 \( A \) 和 \( B \)，其IoU的计算公式为：

\[
\text{IoU}(A, B) = \frac{\text{Area}(A \cap B)}{\text{Area}(A \cup B)}
\]

其中，\( \text{Area}(A \cap B) \) 是边界框 \( A \) 和 \( B \) 的交集区域面积，\( \text{Area}(A \cup B) \) 是边界框 \( A \) 和 \( B \) 的并集区域面积。IoU值的范围为[0, 1]，值越大表示两个边界框重叠的程度越高。通过IoU值，可以判断两个检测框是否有足够的重叠程度，从而决定它们是否属于同一个目标。

在多传感器融合的目标检测中，IoU被用于关联来自不同传感器的检测结果。例如，当激光雷达和相机分别检测到同一目标时，可以通过计算它们的检测框的IoU值来判断是否为同一个目标。如果IoU值大于某一预定阈值（本实验设定为0.5），则认为这两个检测结果属于同一个目标。
### 5.2.2 带权重的非极大值抑制算法
带权重的非极大值抑制（NMS）是一种基于传统NMS的改进方法，主要应用于多传感器目标检测任务中。当多个传感器（如激光雷达和视觉传感器）提供了多个目标框时，传统的NMS算法可能会误删某些重要的检测结果，特别是在目标之间的重叠较大时。而带权重的NMS算法通过引入额外的权重系数（如不同传感器的置信度权重），对重叠较大的目标框进行更智能的选择，从而保留更有意义的检测结果。

该带权重NMS算法的关键步骤如下：
1. 根据目标框的置信度对所有目标框进行降序排序，确保置信度最高的目标框最先被处理。
2. 每次从已排序的目标框列表中选择置信度最高的框作为当前框，并将其添加到最终的检测结果中
3. 对剩余的目标框，计算IoU并根据IoU和置信度的权重规则判断是否保留。
4. 更新待处理框列表，重复以上步骤，直到所有框都被处理。

假设有两个目标框 \(B_1\) 和 \(B_2\)，其置信度分别为 \(c_1\) 和 \(c_2\)，IoU值为 \(\text{IoU}(B_1, B_2)\)，模型分别为 \(m_1\) 和 \(m_2\)（模型1和模型2代表不同的传感器），权重系数为 \(\text{pp_weight}\) 和 \(\text{yolo_weight}\)，则带权重的NMS决策规则为：

关键公式
1. 如果 \( \text{IoU}(B_1, B_2) \leq \text{IoU threshold} \)，则保留 \(B_2\)。
2. 如果 \( m_1 \neq m_2 \)（即两个框来自不同的传感器），则比较权重调整后的置信度：
   - 如果 \( m_1 = 0 \) 且 \( m_2 = 1 \)，则：
     \(
     c_1 < c_2 \times \frac{\text{pp_weight}}{\text{yolo_weight}}
     \)
     如果满足上述条件，则用 \(B_2\) 替换 \(B_1\)。
   - 如果 \( m_1 = 1 \) 且 \( m_2 = 0 \)，则：
     \[
     c_2 > c_1 \times \frac{\text{pp_weight}}{\text{yolo_weight}}
     \]
     如果满足上述条件，则用 \(B_1\) 替换 \(B_2\)。

3. 如果没有满足上述条件，则认为这两个框是重复的，移除置信度较低的框。

设Tc为相机的权重参数，设Tl为雷达的权重参数，权重参数的取值范围在 [0, 1] 之间，权重参数可以根据环境
的不同进行调整，在夜间或者光线不足时给予激光雷达更大地权重。当Tc=1，Tl=0时，完全依赖相机；当Tc=0，Tl=1时，完全依赖激光雷达。

### 5.2.3 融合结果 KITTI数据集
程序接收来自YOLOV5节点和PointPillars节点的检测结果，经过坐标变换后送入融合决策模块。融合结果如如下：

融合检测结果中，红色边界框更倾向于加权后激光雷达检测到的结果，蓝色边界框更倾向于加权后相机的检测结果。边界框上方标注了目标类型和加权后置信度。

由实验结果可以看出，在光线不足的隧道和目标密集的场景中，单一传感器在对远距离及有遮挡的小目标很容易出现误检和漏检的情况。使用激光雷达点云与图像信息融合来检测目标，可以利用各自传感器的优势，能有效的降低目标检测的漏检率，弥补了单一传感器的不足。

## 5.3 软件系统设计与实现
### 5.3.1 算法整体结构设计
基于激光雷达点云与图像信息融合的目标检测主要由 图像与点云数据处理及同步，图像检测，点云检测，决策融合，结果可视化五个模块构成。


图 基于激光雷达点云与图像信息融合的目标检测系统结构

### 5.3.2 机器人操作系统介绍
机器人操作系统（ROS）是由Willow Garage公司于2007年发布的开源机器人操作系统，它为机器人开发提供了一个通用的软件框架。节点（Node）是ROS中最小的进程单元。它是执行某项具体任务的进程或者独立运行的可执行文件，每个节点通过节点管理器（ROS Master）进行管理。节点管理器作为中央管理器负责调度和管理各个节点之间的数据交换。ROS系统构建了一个分布式的进程框架，使得各个进程可以使用多种编程语言，并在不同的机器上运行，通过分布式计算实现协同工作。

图 ROS系统架构

节点可以通过话题（topic）、服务（service）和参数（parameter）进行通讯。话题通信采用发布-订阅模型，是ROS中使用频率较高的通信模式。在这种模式下，一个节点（发布者）将数据发布到特定的话题（topic），而其他节点（订阅者）则可以订阅该话题，从而接收和处理发布的数据。ROS Master作为协调者，负责管理节点间的注册信息，并帮助订阅者与发布者之间建立联系。一旦建立了连接，订阅者便能够接收到发布者发布到该话题的消息。服务通信与话题通信相似，但其特点在于，订阅方在接收数据后会给出反馈，保证消息的实时性和双向交互。而参数服务器则提供了一系列参数，节点可以根据需求访问这些参数，从而简化了参数的管理和共享。

虽然 ROS 自身已经封装了许多原生的数据类型，例如 String、int、char 等，然而其所能包含信息的丰富度存在一定的局限性，当需
要传输不同结构的复杂数据时，原生数据类型的描述就会来带不足，因此可以使用自定义的类型来发布话题。
此处讲自定义消息类型
介绍结果的BoundBoxArray3D格式，并介绍以json结果发布，进入融合模块。


图 本算法ROS节点图

### 5.3.3 ROS 平台实现

## 5.4 实车部署验证
### 5.4.1 无人车硬件平台

介绍阿波罗小车

表 无人车设备选型表 

（1）激光雷达

表 Velodyne技术参数

（2）工业相机
无人车使用的GMSL相机型号为。。。，具有。。。等优点，能够产生流畅的视频流，其主要规格如表。。。所示。相机安装于无人车的顶部，其高度略低于Velodyne激光雷达，距离地面。。。毫米，有助于更远距离地捕捉图像。

表 Camera型号技术参数 

（3）车控域控制器

表 车控域控制器技术参数

### 5.4.2 基于TensorRT 的模型部署
TensorRT是NVIDIA推出的一款高性能深度学习推理框架，专门用于优化和加速深度学习模型的推理过程。通过TensorRT，用户可以在各种硬件平台上高效地执行深度学习模型，尤其是在使用NVIDIA GPU的系统中，能够显著提高模型推理速度和吞吐量。对于本实验平台，TensorRT的部署能够充分发挥硬件优势，满足实时性和低延迟的要求。

在网络层优化方面，TensorRT 运用了卷积融合、相邻元素融合和池化层融合等技术，以减少网络层之间的数据传输和计算复杂度。例如，在卷积融合过程中，当卷积层与批归一化（Batch Normalization, BN）层直接相邻时，TensorRT 会将两者合并为一个层。通过这种融合，可以直接将 BN 的缩放因子和偏移量应用到卷积的权重和偏置中，减少了独立计算步骤。这种合并不仅降低了调用 CUDA 核函数的频次，还优化了推理效率。此外，对于连续的加法或乘法操作，TensorRT 会通过运算符融合将它们合并为更大的计算单元，从而进一步减少运算开销。同样地，TensorRT 对于相邻的池化层会通过合并生成更大的池化层，从而降低了层间的数据交换，提高了整体计算效率。

张量优化是 TensorRT 提升推理性能的另一个重要方面。其优化策略包括张量的拼接与堆叠、权重共享以及张量融合。例如，张量的拼接和堆叠将小维度张量合并为大维度张量，避免了冗余的存储和传输开销；而对于网络中共享相同权重的层，TensorRT 通过在内存中仅存储一次权重，减少了内存的占用。更进一步，TensorRT 可以将相邻层之间的输入和输出张量合并为一个统一的张量，从而减少内存操作和数据传输，优化了推理流程。

TensorRT 的层融合技术体现在垂直融合与水平融合两种策略中。垂直融合将卷积层、BN 层以及激活函数层合并为一个统一的“CBR”（卷积-BN-激活）结构。这一策略通过减少独立调用 CUDA 核函数的次数，从而显著提高了推理效率。在水平融合方面，TensorRT 将连续的具有相同参数配置的层（如多个尺寸相同的“CBR”结构）合并为一个更大的计算单元，进一步降低了层的数量和模型的整体复杂度。这些融合策略有效减少了层间通信开销，并优化了硬件资源的利用率。

此外，TensorRT 在处理模型中特定层时还会采取额外的优化措施。例如，对于 Concat 层，TensorRT 通过提前分配缓存区的方式减少了内存访问的频次，从而进一步降低了推理过程中的资源消耗。这种基于内存优化的策略对嵌入式平台的性能提升尤为重要。
### 5.4.4 算法实验结果 （可视化分析）


# 6 总结与展望